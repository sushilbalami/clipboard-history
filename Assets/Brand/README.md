# Clipboard History Brand Assets

Primary logo:
- `clipboard-history-logo.svg`

Suggested export sizes for app/marketing:
- `1024x1024` (App Store / master)
- `512x512`
- `256x256`
- `128x128`

Quick export with `qlmanage` preview and screenshot tools, or use Figma/Sketch/Illustrator for production PNGs.

For a clean macOS app icon pipeline, keep this SVG as source-of-truth and export a full `AppIcon.appiconset` from it.
